import 'package:flutter/material.dart';

class ImageLabeling extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ImageLabeling',
      style: TextStyle(fontSize: 20.0),
      ),
    );
  }
}